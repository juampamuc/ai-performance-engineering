2026-03-20T04:09:00Z

Open Decisions
- None for the owned directory.

Shared Follow-Up
- If the repo wants this lab surfaced in a shared index or higher-level README, that needs a separate change outside `labs/memory_bandwidth_patterns/`.
- If the repo wants a Blackwell-only TMA tile-copy target in addition to the cp.async-style milestone, that should be added as a separate follow-up rather than folded into this narrow pair.
